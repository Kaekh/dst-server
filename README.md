## dst-server

This docker image provides a Don't starve together Server that will automatically download the last version and startup. 

Environment vars:
- HOME -> install folder (default: /steam)
- STEAMAPPID -> Steam app id (default: 343050)
- STEAMAPP -> app name (default: dst)
- SERVERNAME -> Cluster name (default: DSTServer)
